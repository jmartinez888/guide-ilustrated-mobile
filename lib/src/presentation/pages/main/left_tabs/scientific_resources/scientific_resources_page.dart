import 'package:flutter/material.dart';

class ScientificResourcesPage extends StatelessWidget {
  const ScientificResourcesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('ScientificResourcesPage'));
  }
}
