import 'package:flutter/material.dart';

class HowToDepositPage extends StatelessWidget {
  const HowToDepositPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('HowToDepositPage'));
  }
}
