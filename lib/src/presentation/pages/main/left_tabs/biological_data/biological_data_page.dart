import 'package:flutter/material.dart';

class BiologicalDataPage extends StatelessWidget {
  const BiologicalDataPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('BiologicalDataPage'));
  }
}
